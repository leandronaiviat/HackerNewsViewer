<UserControl x:Class="HackerNewsWPFMVVM.Views.StoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HackerNewsWPFMVVM.Views"
             xmlns:storiesmodel="clr-namespace:HackerNewsWPFMVVM.ModelViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <storiesmodel:StoriesViewModel x:Key="storiesModel"/>
    </UserControl.Resources>
    
    <Grid>
        <!--List of Stories-->
        <ListView Grid.Row="1" ItemsSource="{StaticResource storiesModel}" HorizontalAlignment="center" Width="500">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Title}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
